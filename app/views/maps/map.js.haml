(function(){
\// 位置情報をもつ変数を用意
let pos = new google.maps.LatLng(
\#{@latlng[0]}, //latitude
\#{@latlng[1]}  //longitude
);

mapInstance.setCenter(pos)
let marker = new google.maps.Marker({ map: mapInstance , position: pos});
})()
